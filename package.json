{
  "name": "leotabs",
  "version": "0.13.1",
  "private": true,
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build-overlay.mjs",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
